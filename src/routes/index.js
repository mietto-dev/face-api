/*
	Implementação de rotas. Cada rota idealmente deve importar e implementar um schema
	para otimização de performance e validação dos dados.
*/

const face = require('./face-routes')

module.exports = {
  face
}
